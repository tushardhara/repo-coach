# repo-coach uses only Python stdlib — no runtime dependencies.
# Optional: install tree-sitter for higher-accuracy parsing (not required)
# tree-sitter>=0.20.0
# tree-sitter-python>=0.20.0
# tree-sitter-go>=0.20.0
# tree-sitter-javascript>=0.20.0
